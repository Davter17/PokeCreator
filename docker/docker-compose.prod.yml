services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: prod
    container_name: pokecreator-prod
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - pokecreator-network

networks:
  pokecreator-network:
    driver: bridge
